{
  "name": "silenttalk-root",
  "version": "1.0.0",
  "description": "SilentTalk E2EE Chat Application",
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run build --prefix frontend && npm run preview --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "concurrently": "^8.2.2",
    "multer-storage-cloudinary": "^4.0.0"
  }
}
